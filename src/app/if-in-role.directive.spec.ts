import { IfInRoleDirective } from './if-in-role.directive';

describe('IfInRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new IfInRoleDirective();
    expect(directive).toBeTruthy();
  });
});
